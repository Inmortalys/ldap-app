# LDAP Server Configuration
# Copy this file to .env and update with your settings

# LDAP Server Details
LDAP_SERVER=ldaps://IP
LDAP_PORT=636
LDAP_BASE_DN=DC=DC,DC=ES
# LDAP Search Base (supports multiple bases separated by semicolon)
# Example single base: OU=B,OU=A,DC=DC,DC=ES
# Example multiple bases: OU=B,OU=A,DC=DC,DC=ES;OU=C,OU=A,DC=DC,DC=ES;OU=D,OU=A,DC=DC,DC=ES
LDAP_SEARCH_BASE=OU=B,OU=A,DC=DC,DC=ES

# LDAP Admin Credentials (required for password reset functionality)
LDAP_ADMIN_DN=adminuser
LDAP_ADMIN_PASSWORD=your-admin-password

# JWT Secret for session management (CHANGE THIS IN PRODUCTION!)
JWT_SECRET=your-secret-key-change-this-in-production-use-a-long-random-string
JWT_EXPIRATION=8h

# Server Configuration
PORT=3000
NODE_ENV=development
